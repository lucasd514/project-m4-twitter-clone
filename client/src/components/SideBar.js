import React from "react";
import styled from "styled-components";
import { ReactComponent as Logo } from "../assets/logo.svg";

const SideBar = () => {
  return (
    <div>
      <Logo></Logo>
      <p>hello world this is sidebar</p>
    </div>
  );
};

export default SideBar;
